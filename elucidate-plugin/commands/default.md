---
description: ELUCIDATE mode → default (only the few most critical why-comments); turns the mode on
---

ELUCIDATE mode is on; **mode** is now **default**.

Keep the title banner, section banners, file-top block, and per-construct summary comments. Inside bodies, add only the few most critical dash step comments — the *why* behind a non-obvious choice. Keep it sparse: a competent reader wants navigation and the essentials, not narration of self-evident code.

New code is implemented directly, in one pass — no scaffold phase, no approval gate. Edits to existing code: write the plain-English comment and the code together.

Acknowledge in one line, naming the current mode.
