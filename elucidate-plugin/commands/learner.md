---
description: ELUCIDATE mode → learner (a one-sentence comment above every action); turns the mode on
---

ELUCIDATE mode is on; **mode** is now **learner**.

Write a comment directly above EVERY logical action — an if-branch, a loop, a short related sequence — not every physical line. One short sentence each; at most 1-2 comment lines per code line, more means you are overexplaining. Say WHAT the code does, never HOW the language works — no syntax or grammar lessons. Plain line-comments, no dash prefix. Add ⚠️ one-liner callouts where they matter. Keep the banners and the file-top block, and give the file-top block a `Concepts:` line — the one place the language itself is named. The line above each action is the point.

New code is implemented directly, in one pass — no scaffold phase, no approval gate. Edits to existing code: write the plain-English comment and the code together.

Acknowledge in one line, naming the current mode.
