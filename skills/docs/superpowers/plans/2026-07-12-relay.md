# /relay Self-Relaying Loops Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the `/relay` skill — a wrapper around the built-in `/loop` that runs loops in legs of N iterations, hands off state to a file, and auto-spawns a fresh session per leg to dodge context rot.

**Architecture:** Pure prompt-ware: one new skill folder (`relay/SKILL.md`), a one-line amendment to the preset skill, and ecosystem-kb vault sync. No scripts, no hooks — spawning is a single `Start-Process` line the skill instructs the model to run. Spec: `docs/superpowers/specs/2026-07-11-relay-design.md`.

**Tech Stack:** Claude Code skills (markdown), built-in `/loop` (ScheduleWakeup/CronCreate), PowerShell `Start-Process`, PushNotification tool, ecosystem-audit `audit.py` for verification.

## Global Constraints

- Prompt-ware only: no scripts, no hooks, no background LLM (vault standing rule).
- Repo root is `C:\Users\S.D\.claude` (one git repo holds `skills/` and `ecosystem-kb/`). All paths below are absolute; commit from repo root.
- State file path: `.claude/relay/<slug>.md` in the *target project's* root — never the scratchpad (session-specific).
- Defaults, verbatim from spec: `N=10`, `max_legs: 20`, spawn flags `--permission-mode acceptEdits`.
- Kill switches are mandatory skill content: `/relay stop [slug]`, `max_legs` cap, leg-fencing.
- Plain `/loop` behavior must remain untouched — relay is opt-in per invocation.
- Vault log entries are newest-first: prepend, heading format `## [YYYY-MM-DD] build | <title>`.
- Windows: canonical spawn is PowerShell `Start-Process`; the Bash tool is Git Bash (no native `start`).

---

### Task 1: Smoke-test the spawn assumptions

The whole design leans on two mechanical assumptions. Verify them BEFORE writing the skill that depends on them. No files created; this task gates the rest.

**Files:**
- None (verification only).

**Interfaces:**
- Consumes: nothing.
- Produces: confirmed facts the SKILL.md text in Task 2 states as instructions: (a) `claude` resolves on PATH for `Start-Process`, (b) a slash command passed as the CLI's initial prompt argument actually executes.

- [ ] **Step 1: Verify `claude` resolves on PATH**

Run (PowerShell tool):
```powershell
(Get-Command claude).Source
```
Expected: a path prints (e.g. `...\claude.exe` or an npm `.cmd` shim). If this errors, STOP the plan — spawning cannot work; report to user.

- [ ] **Step 2: Verify a slash command executes as initial prompt**

Run (PowerShell tool, timeout 120000):
```powershell
claude -p "/preset"
```
Expected: output lists the available presets (init, pick-up, catch-up, ...) — proving the CLI parses a leading slash command from the prompt argument. If it instead answers conversationally about "presets" in the abstract, the slash-command-as-prompt assumption is broken: STOP and report; the spawn prompt design needs rework.

- [ ] **Step 3: Verify `Start-Process claude` opens a detached window**

Run (PowerShell tool):
```powershell
Start-Process claude -ArgumentList @('-p','"exit immediately: reply with the single word ok"')
```
Expected: command returns instantly with no error (a console window may flash). This proves `Start-Process` accepts the arg-array form with a quoted multi-word final argument. Any "cannot find file" error → STOP and report.

- [ ] **Step 4: Report findings**

No commit (nothing changed). State the three results in one line each for the reviewer.

---

### Task 2: Write `relay/SKILL.md`

**Files:**
- Create: `C:\Users\S.D\.claude\skills\relay\SKILL.md`

**Interfaces:**
- Consumes: confirmed spawn mechanics from Task 1.
- Produces: the `/relay` skill. Task 3's preset amendment references the invocation form `/relay 30m N=8 /preset ticket-loop`; Task 4's vault page describes this file's behavior. The state-file frontmatter keys (`body`, `interval`, `n`, `leg`, `iter`, `max_legs`, `stop`, `spawn_flags`) are the contract every leg reads.

- [ ] **Step 1: Create the file with this exact content**

````markdown
---
name: relay
description: Self-relaying loops — wraps the built-in /loop so long loops run in legs of N iterations, hand off through a state file, and respawn a fresh session (dodges context rot and uncached re-read cost). Use when the user types /relay (e.g. /relay 10m N=8 /preset ticket-loop), /relay stop, or asks for a long-running loop that will not rot its context.
---

# /relay — self-relaying loops

Wrapper around the built-in `/loop`. The loop runs in **legs** of N
iterations. At a leg boundary the session writes a handoff into the state
file, spawns a fresh Claude session whose injected first prompt is this same
`/relay` command, and stops its own loop. A fresh leg's context is just:
startup hooks + this skill + the handoff.

Relay adds no scheduling — `/loop` owns that:

```
/relay          ← state file + iteration counting + handoff + spawn
   └─ /loop     ← built-in scheduling (ScheduleWakeup / cron)
        └─ body ← /preset ticket-loop etc. — does the work
```

## Usage

```
/relay [interval] [N=10] [mode=accept|bypass] <body>
/relay stop [slug]
```

- `interval` — optional; omitted = self-paced dynamic mode, exactly like bare `/loop`.
- `N` — iterations per leg (default 10).
- `mode` — spawn permission flag: `accept` (default) → `--permission-mode acceptEdits`; `bypass` → `--dangerously-skip-permissions`.
- `<body>` — any loop body. The loop-body contract (state-check first / one unit per firing / breadcrumbs / explicit stop signal) applies unchanged.

Example: `/relay 10m N=8 /preset ticket-loop`

## State file

`.claude/relay/<slug>.md` at the **target project's root** — never the
scratchpad (session-specific; the next leg gets a different one). Slug:
body kebab-cased (`/preset ticket-loop` → `ticket-loop`; an ad-hoc prose
body → a stable 2-3 word kebab summary).

```markdown
---
body: /preset ticket-loop
interval: 10m
n: 8
leg: 1
iter: 0
max_legs: 20
stop: false
spawn_flags: --permission-mode acceptEdits
---

## Handoff

(rewritten at each relay: Done / In flight / Next / Gotchas)

## Breadcrumbs

- [leg 1 / iter 1] one line per firing
```

## Boot — every /relay invocation

1. **`stop` subcommand** → look in `.claude/relay/`: exactly one state file
   (or slug given) → set `stop: true` in it, confirm to user. Several files
   and no slug → list them, ask which. Then done — no loop.
2. Otherwise scan `.claude/relay/*.md` for a file whose `body:` field
   exactly matches this invocation's body (match on `body:`, not a
   re-derived slug — slug derivation may drift between sessions).
   - Match found and `stop: false` → **resume**: read Handoff +
     Breadcrumbs, announce "resuming <slug>, leg <k>". This also revives a
     chain that died mid-leg.
   - No match, or the match has `stop: true` → **init**: write a fresh
     state file (`leg: 1, iter: 0`), frontmatter from the invocation args.
3. Remember this session's own leg number (the file's `leg` at boot).
4. Start the loop: follow the built-in `/loop` skill with the given
   interval and the per-firing contract below wrapped around the body.

## Per-firing contract

1. **Fence.** Read the state file. If `stop: true`, or the file's `leg` ≠
   this session's boot leg → end the loop (dynamic mode: ScheduleWakeup
   stop; cron mode: CronDelete). No spawn. Stopped and orphaned legs die
   here.
2. **Work.** One unit of the body, exactly as the body prescribes.
3. **Book-keep.** Increment `iter`; append
   `- [leg <k> / iter <i>] <one-line summary>` to Breadcrumbs.
4. **Body signaled done** (queue dry / checks green / human needed) → set
   `stop: true`, rewrite Handoff with the closing state, stop the loop. No
   spawn.
5. **`iter == n`** → relay sequence.

## Relay sequence

1. Rewrite `## Handoff` — Done / In flight / Next / Gotchas, written for a
   cold reader: the next leg sees only this. Prune Breadcrumbs to the leg
   just finished. Set `iter: 0`, increment `leg`.
2. New `leg` > `max_legs` → set `stop: true`, PushNotification
   "relay: <slug> hit max_legs", stop the loop. No spawn.
3. Spawn the next leg (PowerShell tool, from the project root — the new
   session must inherit this cwd). Rebuild args from frontmatter
   (`spawn_flags` + `interval` + `n` + `body`); the quoted final element
   keeps the /relay command a single argument:

   ```powershell
   Start-Process claude -ArgumentList @('--permission-mode','acceptEdits','"/relay 10m N=8 /preset ticket-loop"')
   ```

4. PushNotification: "relay: leg <k> spawned for <slug>" (load the tool
   via ToolSearch if deferred).
5. Stop own loop and tell the user this window is now an idle husk — safe
   to close.

## Kill switches

- `/relay stop [slug]` — or hand-edit `stop: true` into the state file
  from any session or editor; every leg checks it every firing and at boot.
- `max_legs` (default 20) — relay sequence step 2.
- Leg-fencing — per-firing contract step 1; duplicate/orphan legs
  self-terminate.

## Sharp edges

- **Permissions.** The spawned session runs unattended. Default
  `acceptEdits`; an allowlist gap parks the leg silently until a human
  looks at its window. `mode=bypass` spawns with
  `--dangerously-skip-permissions` — the user's explicit per-loop call,
  never the default.
- **Crash mid-leg** → the chain dies silently. No watchdog, by design (no
  hooks / no background LLM). The per-relay PushNotification is the
  observability. Re-running the same /relay command revives the chain.
- **Spawn failure** (claude not on PATH, etc.) → PushNotification, set
  `stop: true`, no retry.
````

- [ ] **Step 2: Verify frontmatter parses and required sections exist**

Run (PowerShell tool):
```powershell
python -c "import yaml,io; t=io.open(r'C:\Users\S.D\.claude\skills\relay\SKILL.md',encoding='utf-8').read(); fm=yaml.safe_load(t.split('---')[1]); print(fm['name'], len(fm['description'])>50)"
```
Expected: `relay True`

Run (Grep tool or PowerShell):
```powershell
Select-String -Path "C:\Users\S.D\.claude\skills\relay\SKILL.md" -Pattern '^## (Usage|State file|Boot|Per-firing contract|Relay sequence|Kill switches|Sharp edges)' | Measure-Object | Select-Object -ExpandProperty Count
```
Expected: `7` (Boot heading matches via prefix).

- [ ] **Step 3: Commit**

```powershell
git -C "C:\Users\S.D\.claude" add skills/relay/SKILL.md; git -C "C:\Users\S.D\.claude" commit -m "feat(relay): self-relaying loop skill — legs of N iterations + handoff + auto-spawn"
```
Expected: 1 file changed.

---

### Task 3: Amend preset skill — loop bodies mention relay

**Files:**
- Modify: `C:\Users\S.D\.claude\skills\preset\SKILL.md` (the `## Loop bodies` section, ends line 39)

**Interfaces:**
- Consumes: invocation form from Task 2 (`/relay 30m N=8 /preset ticket-loop`).
- Produces: nothing downstream; user-facing discoverability only.

- [ ] **Step 1: Append one sentence to the `## Loop bodies` paragraph**

In `C:\Users\S.D\.claude\skills\preset\SKILL.md`, the Loop bodies paragraph currently ends:

```
...`health` is loopable as-is (`/loop 1d /preset health`) — it's naturally idempotent.
```

Append after that sentence, same paragraph:

```
Long chains: wrap with `/relay` (`/relay 30m N=8 /preset ticket-loop`) — same body, but the loop relays to a fresh session every N firings via a handoff file, so context never rots.
```

- [ ] **Step 2: Verify**

Run:
```powershell
Select-String -Path "C:\Users\S.D\.claude\skills\preset\SKILL.md" -Pattern '/relay 30m N=8'
```
Expected: one match in the Loop bodies section.

- [ ] **Step 3: Commit**

```powershell
git -C "C:\Users\S.D\.claude" add skills/preset/SKILL.md; git -C "C:\Users\S.D\.claude" commit -m "docs(preset): loop bodies note /relay wrapper"
```
Expected: 1 file changed.

---

### Task 4: Vault sync — relay page, index, log, preset page

**Files:**
- Create: `C:\Users\S.D\.claude\ecosystem-kb\wiki\skills\relay.md`
- Modify: `C:\Users\S.D\.claude\ecosystem-kb\index.md` (Skills list)
- Modify: `C:\Users\S.D\.claude\ecosystem-kb\log.md` (prepend entry)
- Modify: `C:\Users\S.D\.claude\ecosystem-kb\wiki\skills\preset.md` (decision amendment)

**Interfaces:**
- Consumes: Task 2's skill behavior (described, not duplicated).
- Produces: vault pages Task 5's audit reconciles against the skills folder.

- [ ] **Step 1: Create `wiki/skills/relay.md` with this exact content**

```markdown
---
type: skill
updated: 2026-07-12
tags: [skill, loops, handoff]
source: built 2026-07-12; spec skills/docs/superpowers/specs/2026-07-11-relay-design.md
---

# relay

Self-relaying loops — `/relay [interval] [N=10] [mode=accept|bypass] <body>`
wraps the built-in `/loop` so a long loop runs in **legs** of N iterations.
At each leg boundary the session rewrites the Handoff section of
`.claude/relay/<slug>.md` (project-local; scratchpad is session-specific),
spawns a fresh session via `Start-Process claude` with the same `/relay`
command injected as first prompt, and stops its own loop. Fresh leg =
startup hooks + skill + handoff only — context rot and uncached full-history
re-reads (5-min cache TTL) reset every leg.

Layering: relay owns state file + counting + handoff + spawn; `/loop` owns
scheduling; the body (usually a [[preset]] loop body) owns the work and its
loop-body contract, unchanged.

Kill switches, mandatory because spawn is automated: `/relay stop [slug]`
(any session or editor — it is a file edit), `max_legs: 20` cap +
PushNotification, and leg-fencing (a firing whose session leg ≠ file leg
dies silently — orphans self-terminate). Body-signaled done also sets
`stop: true`, so a finished chain never respawns; re-running the same
command revives a mid-leg crash (resume matches on the `body:` field, not
re-derived slug).

Sharp edge: spawned legs run unattended — default spawn flag
`--permission-mode acceptEdits`; an allowlist gap parks the leg silently.
`mode=bypass` (`--dangerously-skip-permissions`) is explicit per-loop
opt-in, never default. Crash mid-leg dies silently by design: no watchdog
hook (standing no-hooks/no-background-LLM rule); the per-relay
PushNotification is the observability.

Decision note: partial, conscious revisit of the 2026-07-10 "user drives
loops explicitly" rejection of cron routines — spawning is automated, but
the user still starts every chain, picks N and the permission mode, and
holds three kill switches.
```

- [ ] **Step 2: Add relay to `index.md` Skills list**

In `C:\Users\S.D\.claude\ecosystem-kb\index.md`, after the line
`- [[preset]] — ...` in the `## Skills` section, insert:

```markdown
- [[relay]] — self-relaying /loop wrapper: legs of N iterations, handoff file, auto-spawned fresh session per leg (kills context rot in long loops)
```

- [ ] **Step 3: Prepend log entry**

In `C:\Users\S.D\.claude\ecosystem-kb\log.md`, entries are newest-first.
Insert directly above the current topmost `## [` heading:

```markdown
## [2026-07-12] build | /relay self-relaying loops

Brainstorm build (spec 2026-07-11). New skill `~/.claude/skills/relay/` —
wraps built-in `/loop` in legs of N iterations (default 10); at leg end:
rewrite Handoff in project-local `.claude/relay/<slug>.md`, `Start-Process
claude` a fresh session with the same `/relay` command injected, stop own
loop. Fixes context rot + uncached re-read cost of long single-session
loops (ScheduleWakeup and CronCreate are both session-bound — nothing
in-session can outlive it, hence spawn). Kill switches: `/relay stop`,
`max_legs: 20`, leg-fencing. Spawned legs run unattended → default spawn
flag `--permission-mode acceptEdits`, `mode=bypass` explicit only. No
watchdog hook by design (standing rule). Conscious partial revisit of the
2026-07-10 "user drives loops explicitly" decision — spawn automated, user
keeps start/stop/caps. [[preset]] loop bodies unchanged, wrappable as-is.
Synced [[relay]] (new), [[preset]], index.

```

- [ ] **Step 4: Amend `wiki/skills/preset.md`**

Append to the end of `C:\Users\S.D\.claude\ecosystem-kb\wiki\skills\preset.md`:

```markdown

2026-07-12: loop bodies gained a wrapper — [[relay]] runs any of them in
relaying legs (`/relay 30m N=8 /preset ticket-loop`). Amends, not reverses,
the 2026-07-10 "user drives loops explicitly" note: spawn is automated, the
user still owns start, N, permission mode, and stop.
```

Also update the page's frontmatter `updated:` field to `2026-07-12`.

- [ ] **Step 5: Verify wikilink integrity**

Run:
```powershell
Select-String -Path "C:\Users\S.D\.claude\ecosystem-kb\index.md","C:\Users\S.D\.claude\ecosystem-kb\wiki\skills\preset.md" -Pattern '\[\[relay\]\]' | Measure-Object | Select-Object -ExpandProperty Count
```
Expected: `2` or more.

- [ ] **Step 6: Commit**

```powershell
git -C "C:\Users\S.D\.claude" add ecosystem-kb/wiki/skills/relay.md ecosystem-kb/index.md ecosystem-kb/log.md ecosystem-kb/wiki/skills/preset.md; git -C "C:\Users\S.D\.claude" commit -m "docs(ecosystem-kb): sync vault for /relay build"
```
Expected: 4 files changed.

---

### Task 5: Reconcile audit + manual acceptance handoff

**Files:**
- None created; runs the existing checker and writes nothing.

**Interfaces:**
- Consumes: Task 2's skill folder + Task 4's vault pages.
- Produces: clean audit evidence + the manual test script for the user (live relay needs real sessions; cannot run inside plan execution).

- [ ] **Step 1: Run the ecosystem audit**

Run:
```powershell
python "C:\Users\S.D\.claude\skills\ecosystem-audit\scripts\audit.py"
```
Expected: no finding that mentions `relay` (no "skill folder without vault page" / "vault page without skill folder" drift). Other pre-existing findings are out of scope — report but do not fix.

- [ ] **Step 2: Hand the user the manual acceptance test**

Print this checklist verbatim as the final report (do not run it — it needs live interactive sessions):

```
Manual acceptance (run in any scratch project):
1. /relay 1m N=2 /preset ci-babysit <some-open-PR#>   ← or any cheap body
2. Watch two firings complete; at iter==2 expect: Handoff rewritten,
   new terminal window opens and resumes leg 2, old session says "husk".
3. In the new window's project: /relay stop → next firing ends the chain.
4. Check .claude/relay/<slug>.md: stop: true, Handoff coherent for a cold reader.
5. Leg-fencing: restart the chain, hand-edit the file's leg to 99 →
   the running leg's next firing must end silently, no spawn.
6. Confirm plain /loop still behaves normally (no relay contract applied).
```

- [ ] **Step 3: No commit**

Nothing changed in this task.
