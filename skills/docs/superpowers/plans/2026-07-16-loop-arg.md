# loop-arg Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a `/preset loop-arg` self-prompting loop body — seed a GOAL once, each firing does one step toward it and rewrites its own NEXT prompt — and widen relay's cross-leg prose block to collapse to a pointer when the body has external state.

**Architecture:** Pure markdown/instruction work. One new preset body file, edits to two SKILL.md dispatchers, and a vault/routing-sheet map sync. No logic-bearing code, so no unit-test cycle — each task's verification is reading the file back and checking cross-reference/table consistency (grep). The new body inverts `ticket-loop`: instead of a fixed instruction block, step 4 overwrites the body's own next prompt in `.claude/loop-arg.md`; the fixed `goal:` is both the self-prompt anchor and the loop's stop condition.

**Tech Stack:** Markdown. Git (Bash tool). Preset house style (title line → `## Loop-body contract` → `## Steps`), matching `preset/presets/ticket-loop.md` and `ci-babysit.md`.

## Global Constraints

- Spec of record: `docs/superpowers/specs/2026-07-16-loop-arg-design.md` (committed `379b395`). Every task traces to it.
- Working dir root for skill files: `C:\Users\S.D\.claude\skills\` (the git repo). Vault + routing sheet live **outside** the repo: `C:\Users\S.D\.claude\ecosystem-kb\` and `C:\Users\S.D\.claude\CLAUDE.md` — edited but not committed here.
- State file the body owns: `.claude/loop-arg.md` at the **target project root** (never the scratchpad). Slug fixed: `loop-arg`.
- Seed idempotency is the single most important correctness point: when `.claude/loop-arg.md` exists, the body MUST ignore the `/preset` arg (a relay respawn re-injects the original command). Getting this wrong resets GOAL every leg.
- `loop-arg`'s own `## Log` is mandatory (body memory). Relay's prose block (`## Handoff` + `## Breadcrumbs` + Gotchas) is what becomes optional — never confuse the two.
- Relay frontmatter counters (`body/interval/n/leg/iter/max_legs/stop/spawn_flags/binary`) never move — machinery, not memory.
- Commit only skill-repo files (Tasks 1–3). Do NOT `git add` the vault or CLAUDE.md (Task 4) — those are outside this repo and the tree is already dirty by user convention.
- Dirty-tree discipline: each commit stages only its own named files, never `git add -A`.

---

### Task 1: Create the loop body `preset/presets/loop-arg.md`

**Files:**
- Create: `preset/presets/loop-arg.md`
- Reference (read-only, do not edit): `preset/presets/ticket-loop.md` (house style)

**Interfaces:**
- Consumes: nothing (self-contained instruction file).
- Produces: the `loop-arg` preset name, invoked as `/preset loop-arg "<goal>"`, `/loop /preset loop-arg "<goal>"`, `/relay N=8 /preset loop-arg "<goal>"`. Owns state file `.claude/loop-arg.md` with frontmatter `goal:` + `stop:` and sections `## Next`, `## Log`. Task 2 references the name + one-line purpose; Task 3 references `.claude/loop-arg.md` as an external state store; Task 4 records it.

- [ ] **Step 1: Write the file**

Create `preset/presets/loop-arg.md` with exactly this content:

```markdown
# loop-arg — self-prompting loop body: pursue one GOAL, writing your own next step each firing

Loop body. Seed a GOAL once, then each firing does ONE concrete step toward it and rewrites the prompt for the firing after it. The prompt is data — a living file — not a fixed preset. Fire via `/preset loop-arg "<goal>"` (seed / one-shot), `/loop /preset loop-arg "<goal>"` (self-paced), or `/relay N=8 /preset loop-arg "<goal>"` (immortal; always run an unattended chain under `max_legs`, never bare `/loop`). This is the inversion of `ticket-loop`: the last step rewrites the body's own next prompt.

## State file — `.claude/loop-arg.md`

At the **target project root** (never the scratchpad — session-specific). Slug fixed: `loop-arg`.

```markdown
---
goal: <fixed — set once at seed from the /preset arg, never rewritten>
stop: false
---

## Next
<the concrete next step written for yourself last firing>

## Log
- [firing 1] one line: what got done
```

`goal:` is the anchor — written once, never touched again. `## Next` is overwritten every firing (the self-prompt). `## Log` is append-only, one line per firing — this is the body's memory; never drop it.

## Loop-body contract

Check state first (the seed guard + goal check make every firing idempotent — a respawn never re-seeds or redoes finished work), do one unit, leave the breadcrumb in `## Log` where the next firing reads it, and signal the loop the moment the goal is met.

## Steps

1. **Seed guard.** `.claude/loop-arg.md` missing → this is firing 1: write `goal:` from the `/preset loop-arg "<goal>"` argument (if the arg is vague, sharpen it into a statement with a checkable done-condition before writing), set `## Next` to the first concrete step, initialize an empty `## Log`, then end the firing. File exists → **ignore the arg** (a relay respawn re-injects the original command; re-seeding would reset GOAL) and read the file.
2. **Goal check.** Read `goal:` and `## Log`. Goal met? → set `stop: true`, append a closing `## Log` line, and signal the loop to stop: dynamic `/loop` → end it (ScheduleWakeup stop); fixed interval → say "goal met, cancel the loop"; under `/relay` → also set the relay state file's `stop: true`. Do NOT rewrite `## Next`. Done.
3. **Do NEXT.** Execute exactly what `## Next` says — ONE unit of work. Never chain a second step; the runner re-fires.
4. **Self-prompt.** Decide the next concrete step toward GOAL given what `## Log` now shows, and **overwrite `## Next`** with it. Append one `## Log` line for the step just completed.

One step per firing, then done. GOAL is the fixed anchor; NEXT is the moving part.
```

- [ ] **Step 2: Verify the file matches house style and is self-consistent**

Run: `head -1 preset/presets/loop-arg.md && grep -nE '^## (Loop-body contract|Steps|State file)' preset/presets/loop-arg.md`

Expected: line 1 is the `# loop-arg — …` title; the three `##` section headers are present. Confirms it follows the `ticket-loop.md` shape (title → contract → steps) plus the state-file schema block.

- [ ] **Step 3: Verify the seed-idempotency guard is present (the critical correctness point)**

Run: `grep -n 'ignore the arg' preset/presets/loop-arg.md`

Expected: one match in Step 1 (seed guard). If absent, the body will reset GOAL on every relay respawn — stop and fix before committing.

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/S.D/.claude/skills"
git add preset/presets/loop-arg.md
git commit -m "feat(preset): add loop-arg self-prompting loop body"
```

---

### Task 2: Register `loop-arg` in `preset/SKILL.md`

**Files:**
- Modify: `preset/SKILL.md` (loop-bodies table row + loop-bodies prose paragraph)

**Interfaces:**
- Consumes: the `loop-arg` name + purpose from Task 1.
- Produces: `/preset` (bare) now lists `loop-arg`; the dispatcher documents it as a loop body.

- [ ] **Step 1: Add the table row**

In `preset/SKILL.md`, the "Available presets" table ends with the `ci-babysit` row (`| \`ci-babysit\` | loop body | Watch a PR's checks…`). Add immediately after it:

```markdown
| `loop-arg`    | loop body | Self-prompting: seed a GOAL once (`/preset loop-arg "<goal>"`), each firing does one step toward it and rewrites its own `## Next` in `.claude/loop-arg.md`; goal met → stop. The prompt is data, not a fixed preset. |
```

- [ ] **Step 2: Extend the loop-bodies prose**

In the `## Loop bodies` section, the paragraph currently names `ticket-loop` and `ci-babysit`. Append this sentence to the end of that paragraph (before the `Long chains:` sentence if present, else at paragraph end):

```markdown
`loop-arg` is the self-prompting body: unlike the others its instruction is not fixed — it seeds a GOAL into `.claude/loop-arg.md` on the first firing, then each firing does one step and overwrites its own next prompt, stopping when the goal is met (always run an unattended `loop-arg` chain under `/relay`'s `max_legs`, never bare `/loop`).
```

- [ ] **Step 3: Verify both edits landed and the table stays aligned**

Run: `grep -n 'loop-arg' preset/SKILL.md`

Expected: at least two matches — the table row and the prose sentence. Eyeball the table row: same `| name | slot | purpose |` column count as the `ci-babysit` row above it.

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/S.D/.claude/skills"
git add preset/SKILL.md
git commit -m "docs(preset): register loop-arg loop body"
```

---

### Task 3: Widen relay's prose-block rule in `relay/SKILL.md`

**Files:**
- Modify: `relay/SKILL.md` (State file section, Per-firing contract step 3, Relay sequence step 1)

**Interfaces:**
- Consumes: the concept of a body with external state (`.claude/loop-arg.md`, tracker tickets, PR comments) from Task 1 + the spec's relay-integration section.
- Produces: the documented rule that the whole `## Handoff` + `## Breadcrumbs` + Gotchas prose block collapses to a `state:` pointer when the body has external durable state; the ad-hoc-prose body keeps it in full.

- [ ] **Step 1: Add the rule to the State file section**

In `relay/SKILL.md`, the `## State file` section shows the state-file template ending with a `## Breadcrumbs` example. Immediately after that code block, add:

```markdown
**Prose block is optional (external-state bodies).** The `## Handoff` +
`## Breadcrumbs` (+ any Gotchas) prose is the cold-reader memory for the next
leg — and the whole file is re-read into every fresh leg, so it is a recurring
per-leg context cost. When the body has its **own durable external state**
(`loop-arg` → `.claude/loop-arg.md`; `ticket-loop` → tracker tickets;
`ci-babysit` → PR checks + comments), that prose block is pure duplication and
collapses to a one-line pointer:

    ## Handoff
    state: .claude/loop-arg.md      # GOAL + NEXT + Log live here

When the body has **no** external store (an ad-hoc prose body like
`/relay "refactor auth until tests pass"`), the full Done / In-flight / Next /
Gotchas block is **required** — it is the chain's only cross-leg memory. The
frontmatter counters never move either way — they are machinery, not memory.
```

- [ ] **Step 2: Make per-firing contract step 3 conditional**

In the `## Per-firing contract` section, step 3 currently reads:

> 3. **Book-keep.** Increment `iter`; append `- [leg <k> / iter <i>] <one-line summary>` to Breadcrumbs.

Replace it with:

```markdown
3. **Book-keep.** Increment `iter`. Append a one-line summary where the next
   leg will read it: `- [leg <k> / iter <i>] <summary>` to `## Breadcrumbs`
   **for an ad-hoc prose body**; **for an external-state body the body already
   logs its own progress** (`loop-arg` step 4 appends to `## Log`), so relay
   keeps no second trail.
```

- [ ] **Step 3: Make relay-sequence step 1 conditional**

In the `## Relay sequence` section, step 1 currently reads (approximately):

> 1. Rewrite `## Handoff` — Done / In flight / Next / Gotchas, written for a cold reader: the next leg sees only this. Prune Breadcrumbs to the leg just finished. Set `iter: 0`, increment `leg`.

Replace it with:

```markdown
1. **External-state body** → the `## Handoff` is just the `state:` pointer;
   there is nothing to rewrite (the body's own store carries Done/Next). **Ad-hoc
   prose body** → rewrite `## Handoff` (Done / In flight / Next / Gotchas) for a
   cold reader — the next leg sees only this — and prune `## Breadcrumbs` to the
   leg just finished. Either way: set `iter: 0`, increment `leg`.
```

- [ ] **Step 4: Verify the three edits are consistent**

Run: `grep -nE 'external-state|state: .claude/loop-arg.md|Prose block is optional' relay/SKILL.md`

Expected: matches in all three sections (State file, Per-firing contract, Relay sequence). Confirm no remaining sentence claims Breadcrumbs are always written unconditionally.

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/S.D/.claude/skills"
git add relay/SKILL.md
git commit -m "docs(relay): prose block collapses to pointer for external-state bodies"
```

---

### Task 4: Sync the ecosystem map (vault + routing sheet)

**Files:**
- Modify: `C:\Users\S.D\.claude\ecosystem-kb\wiki\skills\preset.md` (new loop body note)
- Modify: `C:\Users\S.D\.claude\ecosystem-kb\wiki\skills\relay.md` (prose-block-optional rule)
- Modify: `C:\Users\S.D\.claude\ecosystem-kb\index.md` (preset/relay one-liners if they enumerate bodies)
- Modify: `C:\Users\S.D\.claude\CLAUDE.md` (routing sheet — recurring-task row)

**Interfaces:**
- Consumes: everything from Tasks 1–3.
- Produces: the map matches the territory (standing rule: ecosystem change → sync vault same session). NOT committed here — these files are outside the skills repo.

- [ ] **Step 1: Add a dated note to the preset vault page**

Append to `C:\Users\S.D\.claude\ecosystem-kb\wiki\skills\preset.md` (after the last dated paragraph, matching the page's existing `YYYY-MM-DD:` note style), and bump the frontmatter `updated:` to `2026-07-16`:

```markdown
2026-07-16: third loop body added — `loop-arg`, the self-prompting body. Unlike `ticket-loop`/`ci-babysit` its instruction is not fixed: it seeds a GOAL into `.claude/loop-arg.md` on the first firing, then each firing does one step and overwrites its own `## Next`, stopping when the goal is met. Inversion of `ticket-loop` (step 4 rewrites the body's own prompt). Run unattended chains under [[relay]] `max_legs`.
```

- [ ] **Step 2: Add the prose-block rule to the relay vault page**

Append to `C:\Users\S.D\.claude\ecosystem-kb\wiki\skills\relay.md` and bump `updated:` to `2026-07-16`:

```markdown
Prose-block-optional (2026-07-16) — the `## Handoff` + `## Breadcrumbs` + Gotchas block is re-read into every leg (per-leg context cost). When the body has its own durable external state (`loop-arg` → `.claude/loop-arg.md`, `ticket-loop` → tickets, `ci-babysit` → PR), the whole block collapses to a one-line `state:` pointer — Breadcrumbs/Gotchas were duplicating the body's own trail. Ad-hoc prose bodies keep the full block (their only cross-leg memory). Frontmatter counters never move. Paired with the new [[preset]] `loop-arg` body.
```

- [ ] **Step 3: Update index.md one-liners if they enumerate bodies**

Run: `grep -n 'ticket-loop\|ci-babysit' "C:/Users/S.D/.claude/ecosystem-kb/index.md"`

If the `[[preset]]` line enumerates loop bodies (`ticket-loop/ci-babysit`), add `/loop-arg` to that enumeration. If it does not enumerate them, leave index.md unchanged. (Read the matched line before editing; do not invent an enumeration that is not there.)

- [ ] **Step 4: Update the CLAUDE.md routing sheet**

In `C:\Users\S.D\.claude\CLAUDE.md`, the situation→invoke table has:

> `| Recurring task | \`/loop\` + preset body (ticket-loop, ci-babysit); long chains → \`/relay\` |`

Replace that row's cell with:

```markdown
| Recurring task | `/loop` + preset body (ticket-loop, ci-babysit); autonomous goal-seeking → `loop-arg`; long chains → `/relay` |
```

- [ ] **Step 5: Verify the map is consistent**

Run: `grep -rn 'loop-arg' "C:/Users/S.D/.claude/ecosystem-kb/" "C:/Users/S.D/.claude/CLAUDE.md"`

Expected: matches in `wiki/skills/preset.md`, `wiki/skills/relay.md`, `CLAUDE.md`, and (if it enumerated bodies) `index.md`. This is the map-matches-territory check; no commit (files are outside the skills repo — the user folds them into their normal wrap-up).

---

## Self-Review

**1. Spec coverage** — every spec section maps to a task:
- Spec "What we're building" (one preset + file) → Task 1.
- Spec "State file schema" → Task 1, Step 1 (schema block reproduced verbatim).
- Spec "Per-firing flow" (4 steps) → Task 1, Step 1 (`## Steps` 1–4).
- Spec "Loop-body contract compliance" → Task 1 (`## Loop-body contract` section).
- Spec "Relay integration — prose block optional" → Task 3 (all three relay sections) + Task 1 (`.claude/loop-arg.md` as the external store).
- Spec "Files to create/change" #1 → Task 1; #2 → Task 2; #3 → Task 3; #4 → Task 4 Steps 1–2; #5 → Task 4 Steps 3–4.
- Spec "Sharp edges" (runaway, seed idempotency, goal quality, permissions) → encoded in Task 1 body text (Steps 1–2, arg-sharpening, `max_legs` note) + Task 1 Step 3 verification (idempotency guard).

**2. Placeholder scan** — no TBD/TODO/"handle edge cases"/"similar to Task N". Every file-content step shows the literal text to write.

**3. Type consistency** — the state-file field/section names are identical across all tasks: `goal:`, `stop:`, `## Next`, `## Log` (body); `## Handoff`, `## Breadcrumbs`, `state:` pointer, frontmatter counters (relay). `.claude/loop-arg.md` path spelled identically everywhere. Slug `loop-arg` consistent. No drift.

**No gaps found.**
